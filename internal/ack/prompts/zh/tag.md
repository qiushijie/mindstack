# Tag Extraction

将用户问题映射到知识库现有标签集合。只返回 JSON 数组，不得创造新标签。

---

你正在帮助把用户问题路由到知识库中相关的文档。

用户问题:%s

知识库中可用的标签（每个标签后括号内为使用该标签的文档数量，优先选择文档数量较多的标签）:
%s

请挑选与回答该问题最相关的标签。规则:
- 只返回一个 JSON 字符串数组，标签必须来自上面列表（不得自己创造）
- 优先选择文档数量较多的标签，它们通常更可靠
- 选择 2-5 个标签即可，不要过多
- 如果没有合适的，返回 []

示例:["api","rest"]
