# Keyword Extraction

将用户问题分词为独立的关键词，保持原语言，用于全文搜索。

---

你正在将用户问题拆分为独立的关键词，用于在知识库文档中进行全文搜索。

用户问题: %s

请将问题分词为 3-8 个独立的关键词:
- 保持原语言（中文问题返回中文关键词，英文问题返回英文关键词）
- 去除虚词（如"的""了""如何""什么""实现""可以"等）
- 只保留有实质含义的词

只返回 JSON 字符串数组（不要 markdown 围栏）。示例:["重试","策略","指数退避","超时"]
